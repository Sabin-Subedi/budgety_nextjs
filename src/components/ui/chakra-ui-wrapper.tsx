"use client";

import {
  Box,
  Button,
  Container,
  Flex,
  Grid,
  GridItem,
  IconButton,
  Stack,
} from "@chakra-ui/react";

export const AppBox = Box;
export const AppContainer = Container;
export const AppFlex = Flex;
export const AppGrid = Grid;
export const AppGridItem = GridItem;
export const AppStack = Stack;

export const AppButton = Button;
export const AppIconButton = IconButton;
